# dsh-method-stack：设置页「方法编排」（host 方法引擎 + web 客户端 settings.section）
# 单一安装行：host 通道 + web 客户端共用同一 bundle 入口。
- insert:
    - id: dsh-method-stack
      name: '@dsh-external/dsh-method-stack'
      config:
        enable: true
