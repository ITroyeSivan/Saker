# CVSS 评分与修复建议
1. 评分按 CVSS v3.1 向量逐项定：AV/AC/PR/UI（可利用性）→ S/C/I/A（影响），给完整向量串 + Base 分。
2. 结合业务上下文校准：资产价值（核心数据/边界系统）、暴露面、已有缓解。
3. 修复建议分级：临时缓解（WAF/最小权限/版本）与根本修复（代码层/配置层），并给验证方法（修复后如何复测）。
4. 输出：每漏洞=向量串+分数+一句定级理由+修复两条（可执行）。
纪律：分数要能辩护（写依据）；不为了"好看"压分或抬分。