# Saker 报告交付链路实测（2026-09-19）

## 目的

验证 finding 不只是能在页面看到，还能真正导出为 HTML、JSON 和六字段 Markdown 报告，并经过结构与一致性校验。

## 测试数据

隔离会话：`session-05998b8e-ed50-4d4e-a6dd-24e4918cff01`

注入两条结构完整且 verified 的 finding：

- high：`/admin role` 参数越权，含基线、差分、marker、复核与二次评级。
- medium：`/.git/config` 信息泄露，含请求/响应和独立复核。

## 浏览器导出

在 CFT 中打开 `redteam 成果`，实际点击：

1. `导出 -> 报告包（HTML）`
2. `导出 -> 结构化报告（JSON）`

结果：

| 产物 | 结果 |
|---|---|
| HTML | 11,545 bytes，包含 finding 标题与 `REPORT_EXPORT_IDOR` |
| JSON | 14,124 bytes，含 4 条跨会话 finding |

JSON 经真实校验器检查：

```text
ok schema=saker.redteam.report.v1 version=1 migrated=false warnings=0
```

## 六字段 MD

真实 findings 通过 `scripts/generate-report-drafts.mjs` 落盘：

```text
01-报告导出夹具：-admin-role-参数越权.md
02-报告导出夹具：-.git-config-信息泄露.md
```

两份草稿均含：

- 漏洞/问题 名称
- 漏洞/问题 描述
- 漏洞/问题 等级
- 漏洞/问题 地址
- 测试过程
- 修复建议
- 证据与独立验证

第二次运行全部跳过，未覆盖已有稿。

## 发现并修复

报告草稿把 `impact` 证据等级输出成英文枚举，而成果页显示“影响已证”。现已修复：

```text
impact -> 影响已证
```

并新增回归断言，防止后续再次退化。

## 回归

- 报告草稿测试：`10/10` 通过。
- 主门禁：`37` 套，`2020 ok / 0 fail / 17 skip`，`release-gate: 6/6`。
- 根包升至 `0.3.5`。
- 根包 `lib/report-drafts.mjs` 在源码、真实 profile、隔离 profile 三处哈希一致。
