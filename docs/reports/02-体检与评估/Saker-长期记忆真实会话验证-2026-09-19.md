# Saker 长期记忆真实会话验证（2026-09-19）

## 目的

验证 Saker 自身长期记忆不是只有数据库 API，而是能跨会话注入真实模型上下文并影响回答。

## 测试

工作区：`_ref/memory-e2e-audit`

### 会话一：写入

会话：`session-508ada30-de36-46f5-851e-ab09f73e1be7`

真实模型调用一次 `campaign_memory_write`，写入：

```text
title: 授权靶标标记
content: 本项目授权靶标是 http://127.0.0.1:18081，
         标记是 SAKER_MEMORY_E2E_42。
```

返回 memory id：`cm-mu7gnqf0hxb9b9`

数据库实际记录：

```text
id: cm-mu7gnqf0hxb9b9
workspace: memory-e2e-audit
kind: tactic
title: 授权靶标标记
```

### 会话二：跨会话召回

会话：`session-4f30ffbd-3558-457e-9a0e-7e779bb4f66e`

明确要求“不调用任何工具、不读取文件，只根据本项目长期记忆回答”。模型回答：

```text
授权靶标 URL：http://127.0.0.1:18081
标记：SAKER_MEMORY_E2E_42
依据：本项目长期记忆
```

transcript 中没有 `tool/call`，说明回答来自会话开局注入的项目记忆，而不是再次查询数据库。

## 说明

`usage_count` 仍为 0。代码语义明确：只有 `getMemory` 读取全文时才记“真实使用”，开局注入本身不自动计使用，避免把“注入了但模型未使用”算成命中。本轮用模型是否无需工具且能正确回答来验证实际效果。

## 结论

- 跨会话写入、注入、召回链路真实可用。
- 同一工作区的新会话可以拿到项目长期记忆。
- 本轮不需要修补。
